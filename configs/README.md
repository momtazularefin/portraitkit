# PortraitKit pipeline and benchmark configs go here.
# YAML config files for pipeline stage presets (passport, visa, profile photo),
# model adapter selection, and PortraitBench dataset/degradation settings.
